<h3><%= @page.title %></h3>

<pre>
<%= @page.content %>
</pre>

<br>
  <%= link_to 'Edit', edit_admin_page_path(@page) %> | 
  <%= link_to 'Public View', page_path(@page) %> | 
  <%= link_to 'Back to Listing', admin_pages_path %> | 
  <%= link_to 'Delete', admin_page_path(@page), :method => :delete, :confirm => "Are you sure?" %>
